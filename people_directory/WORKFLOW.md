# MOVED — see the vault

**This contract moved to the vault on 2026-07-08:**
`~/Desktop/AI Safety/AI Safety/PROCESS_DIRECTORY.md`
(sandbox mount: `/sessions/*/mnt/AI Safety--AI Safety/PROCESS_DIRECTORY.md`)

Same content, same Part I–IV structure, same invariant numbering (1–11).
Contracts live in the vault; this repo holds machinery. Do not edit this stub
and do not recreate content here — every reference to
`people_directory/WORKFLOW.md` (skill router, older specs, QC prompts) should
be read as pointing to `PROCESS_DIRECTORY.md` at the vault root.
